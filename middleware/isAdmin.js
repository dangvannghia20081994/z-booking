export default function ({ store, redirect }) {
  if (store.state.user) {
    // const roles = []
    // if (!store.state.auth.user.roles) {
    //   $auth.logout()
    //     .then((res) => {
    //       return redirect('/login')
    //     })
    //     .catch((err) => {
    //       console.error(err)
    //     })
    // }
  }
}
